# Lead submissions log path (optional — defaults to console logging)
# LEADS_LOG_PATH=./leads.json

# SMTP settings (for future email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@email.com
# SMTP_PASS=yourpassword
# LEADS_NOTIFY_EMAIL=sales@accredian.com
